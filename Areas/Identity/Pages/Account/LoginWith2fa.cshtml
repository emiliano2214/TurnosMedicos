@page
@model LoginWith2faModel
@{
    ViewData["Title"] = "Autenticación de dos factores";
}

<h1 class="text-center">@ViewData["Title"]</h1>
<hr />

<p class="text-center">
    Tu inicio de sesión está protegido con una aplicación de autenticación.
    Ingresá tu código de autenticación abajo.
</p>

<div class="row justify-content-center">
    <div class="col-md-5">
        <form method="post" asp-route-returnUrl="@Model.ReturnUrl">
            <input asp-for="RememberMe" type="hidden" />

            <div asp-validation-summary="ModelOnly" class="text-danger" role="alert"></div>

            <div class="form-floating mb-3">
                <input asp-for="Input.TwoFactorCode" class="form-control" autocomplete="off" placeholder="Código de autenticación" />
                <label asp-for="Input.TwoFactorCode" class="form-label">Código de autenticación</label>
                <span asp-validation-for="Input.TwoFactorCode" class="text-danger"></span>
            </div>

            <div class="checkbox mb-3">
                <label asp-for="Input.RememberMachine" class="form-label">
                    <input asp-for="Input.RememberMachine" />
                    Recordar este dispositivo
                </label>
            </div>

            <button type="submit" class="w-100 btn btn-lg btn-primary">Iniciar sesión</button>
        </form>
    </div>
</div>

<p class="text-center mt-3">
    ¿No tenés acceso a tu aplicación autenticadora?
    Podés <a id="recovery-code-login" asp-page="./LoginWithRecoveryCode" asp-route-returnUrl="@Model.ReturnUrl">iniciar sesión con un código de recuperación</a>.
</p>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
